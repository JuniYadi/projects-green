// Super-Linter runs from its container, so expose the project's flat config
// at the repository-owned location it resolves for TypeScript and TSX files.
export { default } from "../../eslint.config.mjs"
