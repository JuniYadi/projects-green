import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Terminal Console",
  description: "Dedicated application container terminal console",
}

export { default } from "./page-client"
